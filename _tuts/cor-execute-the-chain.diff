diff --git a/src/GameApplication.php b/src/GameApplication.php
index 9b87ab6..a00fdb1 100644
--- a/src/GameApplication.php
+++ b/src/GameApplication.php
@@ -107,6 +107,9 @@ class GameApplication
         $fightResultSet->of($winner)->addVictory();
         $fightResultSet->of($loser)->addDefeat();
 
+        $xpBonus = $this->xpBonusHandler->handle($winner, $fightResultSet->of($winner));
+        $winner->addXp($xpBonus);
+
         $this->notify($fightResultSet);
 
         $winner->rest();
