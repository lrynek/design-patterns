diff --git a/src/ChainHandler/CasinoHandler.php b/src/ChainHandler/CasinoHandler.php
index 8302aff..50c5aed 100644
--- a/src/ChainHandler/CasinoHandler.php
+++ b/src/ChainHandler/CasinoHandler.php
@@ -23,6 +23,8 @@ class CasinoHandler implements XpBonusHandlerInterface
 
         // The player wins if rolled a pair
         if ($dice1 === $dice2) {
+            GameApplication::$printer->info('You earned extra XP thanks to the Casino handler!');
+
             return 25;
         }
 
diff --git a/src/ChainHandler/LevelHandler.php b/src/ChainHandler/LevelHandler.php
index 4857957..e4559e0 100644
--- a/src/ChainHandler/LevelHandler.php
+++ b/src/ChainHandler/LevelHandler.php
@@ -13,6 +13,8 @@ class LevelHandler implements XpBonusHandlerInterface
     public function handle(Character $player, FightResult $fightResult): int
     {
         if ($player->getLevel() === 1) {
+            GameApplication::$printer->info('You earned extra XP thanks to the Level handler!');
+            
             return 25;
         }
 
diff --git a/src/ChainHandler/OnFireHandler.php b/src/ChainHandler/OnFireHandler.php
index 8e838bd..1a30c6d 100644
--- a/src/ChainHandler/OnFireHandler.php
+++ b/src/ChainHandler/OnFireHandler.php
@@ -13,6 +13,8 @@ class OnFireHandler implements XpBonusHandlerInterface
     public function handle(Character $player, FightResult $fightResult): int
     {
         if ($fightResult->getWinStreak() >= 3) {
+            GameApplication::$printer->info('You earned extra XP thanks to the OnFire handler!');
+
             return 25;
         }
 
